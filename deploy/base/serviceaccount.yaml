apiVersion: v1
kind: ServiceAccount
metadata:
  name: kadto
  # In stage/prod overlays this is annotated with the GCP service account
  # for GKE Workload Identity:
  #   iam.gke.io/gcp-service-account: kadto-sa@<project>.iam.gserviceaccount.com
automountServiceAccountToken: false
